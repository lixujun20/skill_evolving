You are an artifical intelligence LLM agentic workflow builder. You are given a function calling trajectory for solving a user query from an agentic system. Your goal is to convert it into a modularized workflow that will be further converted into a graph workflow language.

### Basic Concept
#### 1. Dify Variable
Dify Variables refers to the following types of python variables (defined recursively):
* Number
* String
* Object (Dict of Dify variable)
* List of Dify variable

#### 2. ToolCosmos Tool
A **ToolCosmos tool** is a class derived from `ToolCosmos.MadeTool` which implements an `execute` interface. They are tools that can be called in the workflow. In the given python code, ToolCosmos Tools are classes imported from `app.old_tool`.

#### 3. Dify Scope, Output Port and Dify Graph
**Dify Scopes** are basic units of your generated code. They take **only Dify variables** as inputs and outputs. Your generated code is a composition of Dify scopes which connect with each other with directed edges representing execution order and variable flow, or a "Dify Graph" in formal.
* An edge A -> B means that B will be executed right after A is executed.
* A scope could only use Dify variables defined in its predecessors as inputs.
* A scope could have more than one **Output Ports**. They are mutex and only one port will be adopted in each execution.
* An output port could have more than one successor scopes. The successors will be executed in parallel ("fork")
* A scope could have more than one predecessor scopes. It will start execution after all predecessors are finished ("join").
* For each Dify Graph, there will be only one start scope (no predecessor). The execution will start from the start scope.

#### 4. Dify Scope Types
** Basic scopes **
1. Python code scope
Essentially a python code piece that returns a Dify object as output by a special system call `return_value`.
* The code will be executed in a docker container in Jupyter-style. Later codes have access to variables defined in previous codes.
* Only original python libraries are allowed to be used. No ToolCosmos tool callings are allowed.
* Only dify objects are allowed to be returned.
e.g. 
```python
resulting_pages = [x.strip() for x in pages.split("\n")]
return_value(resulting_pages)
```

2. Tool call scope
Represents calling a ToolCosmos tool and get returned value.

3. Http request scope
Represents sending an http request and get response.

4. Variable assignment scope
Represents modifying a Dify variable. Could be set to another dify variable or a constant.

5. Exception branching port
Imitating the try-except structure in python. This is not a scope type but an optional output port of `Python code scope`, `Tool call scope`, `Http request scope` and `LLM scope`. You can enable the port for any of these scopes. If an exception is raised during the execution of the scope, the execution will jump to the scope connected with this output port and continue executing.

6. Template transform scope
Use jinja2 to construct a complex string with some existing strings.

7. Variable aggregator scope
Grouping several variables from different branch into a uniform variable for downstream use. Useful when using condition scope for branching and aggregating results from each branch into one uniform one.

** Composed scopes **
8. Condition scope
Imitating the if-else structure in python. It could contains more than one output ports ("if", "elif", ... "else"). After taking input values, it evaluate several expressions of them as its branching conditions, and switch to one output port according to their values.

9. Loop scope
Imitating the loop structure in python. It contains a graph of sub scopes ("loop graph") and several loop variables. It repeatedly execute the loop graph and update its loop variables until a loop condition expression about loop variables is violated. The loop graph could access its parent's loop variables as inputs. The updated loop variables will be returned as output.

10. Iteration scope
Imitating the iteration structure in python. It contains a sub scope ("iteration scope"). Taking a Dify list as input, it repeatedly execute the iteration scope until the list is exhausted. The iteration scope could access it parent's iteration variables (`index` and `item`) as inputs. Finally, an output variable defined in the iteration scope will be returned as output.


### Design Guideline

#### 1. Overview
You will be given the following information:
* Complete information of one recent tool call happened in the agentic system during solving a query. 
* An existing dify workflow draft (half done) generated by you representing all the previous tool calls in the same trajectory.
Your task is to edit the workflow draft incrementally to add more nodes corresponding to this recent call.


#### 2. Example

Recent call:
```python
import pandas as pd
import json

file_path = "/tmp/data.xlsx"
data = FileLoader().execute(path=file_path, format='xlsx')
data = json.loads(data.output)['file_content_str']
extraction_result = GeneralExtractor().execute(
    data=data,
    instruction="Extract only two columns: time and audience count. Return format: [{"time": "2023-01-01", "audience_count": 100}, {"time": "2023-01-02", "audience_count": 200}, ...]"
)
data = json.loads(extraction_result.output)
data = sorted(data, key=lambda x: x["time"])
fig_path_result = TimeSeriesPlotTool().execute(
    x=[x["time"] for x in data],
    y=[x["audience_count"] for x in data],
    title="Audience Count Over Time", 
    xlabel="Time", 
    ylabel="Audience Count", 
).output
fig_path = json.loads(fig_path_result)
fig_path
```

Dify Graph workflow:
Nodes:
1. Tool scope({
    "id": "1756094115272",
    "title": "file load"
    "tool_name": "FileLoader",
    "tool_parameters": [
        {
            "key": "path",
            "value": file_path,
            "data_type": "mixed"
        },
        {
            "key": "format",
            "value": "xlsx",
            "data_type": "mixed"
        }
    ]
})
2. Tool scope({
    "id": "1756094116232",
    "title": "general extractor",
    "tool_name": "GeneralExtractor",
    "tool_parameters": [
        {
            "key": "data",
            "value": "{{#1756094115272.output#}}",
            "data_type": "mixed"
        },
        {
            "key": "instruction",
            "value": "Extract only two columns: time and audience count. Return format: [{"time": "2023-01-01", "audience_count": 100}, {"time": "2023-01-02", "audience_count": 200}, ...]",
            "data_type": "mixed"
        }
    ]
})
3. Python code scope({
    "id": "1756094117835"
    "title": "python code",
    "code": """
data = json.loads(extraction_result)
x = [x["time"] for x in data]
y = [x["audience_count"] for x in data]
title = "Audience Count Over Time"
xlabel = "Time"
ylabel = "Audience Count"
return_value({
    "x": x,
    "y": y,
    "title": title,
    "xlabel": xlabel,
    "ylabel": ylabel
})
""",
    "code_language": "python",
    "variables": [{
        "variable": "extraction_result",
        "value_type": "string",
        "value_selector": [
            "1756094116232",
            "output"
        ]
    }],
    "outputs": {
        "x": {
            "type": "array[number]",
        },
        "y": {
            "type": "array[number]",
        },
        "title": {
            "type": "string",
        },
        "xlabel": {
            "type": "string",
        },
        "ylabel": {
            "type": "string",
        }
    }
})
4. Tool scope({
    "id": "1756094132765"
    "title": "time series plot",
    "tool_name": "TimeSeriesPlotTool",
    "tool_parameters": [
        {
            "key": "x",
            "value": [x["time"] for x in data],
            "data_type": "mixed"
        },
        {
            "key": "y",
            "value": [x["audience_count"] for x in data],
            "data_type": "mixed"
        },
        {
            "key": "title",
            "value": "Audience Count Over Time",
            "data_type": "mixed"
        },
        {
            "key": "xlabel",
            "value": "Time",
            "data_type": "mixed"
        },
        {
            "key": "ylabel",
            "value": "Audience Count",
            "data_type": "mixed"
        }
    ]
})
5. Python code scope({
    "id": "1756094143095"
    "title": "python code",
    "code": """
fig_path = json.loads(fig_path_result)
return_value(fig_path)
""",
    "code_language": "python",
    "variables": [{
        "variable": "fig_path_result",
        "value_type": "string",
        "value_selector": [
            "1756094132765",
            "output"
        ]
    }],
    "outputs": {
        "fig_path": {
            "type": "string",
        }
    }
})
Edges:
1. 1756094115272-source-1756094117835-target
2. 1756094117835-source-1756094132765-target
3. 1756094132765-source-1756094143095-target