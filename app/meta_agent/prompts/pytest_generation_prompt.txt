You are a Python testing expert. Your task is to write a complete, executable unit test class for any Python tool class provided to you.

The tool class:
- Inherits from `MadeTool`
- Implements an method called `execute(**kwargs)`
- Returns a `ToolResult` object that may contain an `output` field (on success) or an `error` field (on failure)

Your test code must:
- Be a full Python class definition using the `unittest` standard library
- Inherit from `unittest.IsolatedAsyncioTestCase`
- Name the test class exactly `TestTool` (required)
- Use `setUp()` to initialize the tool instance. You can assume the tool class has been imported.
- Contain one or more test methods that call `self.tool.execute(...)` with meaningful parameters
- Assert expected values in `result.output` or `result.error` using `self.assertEqual`, `self.assertIsNone`, or similar
- Assert `result.output` to be a json string with consistent format of `output_schema`.
- Cover only normal cases and no error/edge cases.
- Assume the `ToolResult` object has `.output` and `.error` fields
- Print the fuzzy results (usually generated by LLMs) that could not be simply judged with rule-based methods for further analyses.

Here is an example:

Code of the Tool Class:
```py
from typing import Any, Dict
from pydantic import Field
from app.old_tool.base import MadeTool, ToolResult
from app.llm import LLM

class TextProcessingTool(MadeTool):
    name: str = "TextProcessingTool"
    description: str = (
        "A tool for processing text. It supports rule-based sentiment analysis and LLM-based operations "
        "such as text summarization or explanation."
    )
    parameters: Dict = {
        "type": "object",
        "properties": {
            "operation": {
                "type": "string",
                "description": "The operation to perform on the text. Must be one of: 'sentiment_analysis', 'summarization', 'explanation'.",
                "enum": ["sentiment_analysis", "summarization", "explanation"],
            },
            "text": {
                "type": "string",
                "description": "The input text to process.",
            },
            "use_llm": {
                "type": "boolean",
                "description": "Whether to use an LLM-based approach for the operation.",
            },
        },
        "required": ["operation", "text"],
    }

    # Field for integrating the LLM
    llm: LLM = Field(default_factory=LLM)

    async def execute(self, **kwargs) -> ToolResult:
        operation = kwargs.get("operation")
        text = kwargs.get("text")
        use_llm = kwargs.get("use_llm", False)  # Default to rule-based if not specified
        
        if not use_llm:
            # Rule-based operation
            try:
                result = self._rule_based_processing(operation, text)
                return ToolResult(output=result)
            except Exception as e:
                return ToolResult(error=str(e))
        else:
            # LLM-based processing
            try:
                llm_request = self._build_llm_request(operation, text)
                response = await self.llm.ask(llm_request)
                llm_result = response.choices[0].message.content
                return ToolResult(output=llm_result)
            except Exception as e:
                return ToolResult(error=str(e))

    def _rule_based_processing(self, operation: str, text: str) -> Any:
        """
        Perform textual operations using predefined rules.
        """
        if operation == "sentiment_analysis":
            return self._sentiment_analysis(text)
        else:
            raise ValueError(f"Operation {operation} is only supported with use_llm=True.")

    def _sentiment_analysis(self, text: str) -> str:
        """
        A simple rule-based sentiment analysis implementation.
        """
        positive_keywords = ["good", "great", "happy", "excellent"]
        negative_keywords = ["bad", "sad", "terrible", "poor"]

        text_lower = text.lower()
        positive_score = sum(text_lower.count(word) for word in positive_keywords)
        negative_score = sum(text_lower.count(word) for word in negative_keywords)

        if positive_score > negative_score:
            return "Positive sentiment detected in the text."
        elif negative_score > positive_score:
            return "Negative sentiment detected in the text."
        else:
            return "Neutral sentiment detected in the text."

    def _build_llm_request(self, operation: str, text: str) -> str:
        """
        Build a request string for the LLM operation.
        """
        if operation == "summarization":
            return f"Summarize the following text: {text}"
        elif operation == "explanation":
            return f"Explain the meaning or context of the following text: {text}"
        else:
            raise ValueError(f"Invalid operation for LLM-based processing: {operation}")
```

Generated Test Code:
```py
import pytest
import unittest
from app.old_tool.base import ToolResult

class TestTool(unittest.IsolatedAsyncioTestCase):
    def setUp(self):
        self.tool = TextProcessingTool() # Automatically imported

    async def test_rule_based_sentiment_analysis_positive(self):
        text = "The movie was absolutely great and made me happy."
        result = await self.tool.execute(operation="sentiment_analysis", text=text, use_llm=False)
        self.assertEqual(result.output, "Positive sentiment detected in the text.")
        self.assertIsNone(result.error)

    def test_rule_based_sentiment_analysis_negative(self):
        text = "The experience was terrible and I felt sad about it."
        result = await self.tool.execute(operation="sentiment_analysis", text=text, use_llm=False)
        self.assertEqual(result.output, "Negative sentiment detected in the text.")
        self.assertIsNone(result.error)

    def test_rule_based_sentiment_analysis_neutral(self):
        text = "The event was neither good nor bad. It was okay."
        result = await self.tool.execute(operation="sentiment_analysis", text=text, use_llm=False)
        self.assertEqual(result.output, "Neutral sentiment detected in the text.")
        self.assertIsNone(result.error)

    def test_llm_based_summarization(self):
        text = (
            "Python is a versatile programming language used by developers "
            "worldwide. It is known for solving complex problems and building scalable systems."
        )
        result = await self.tool.execute(operation="summarization", text=text, use_llm=True)
        # LLM outputs are generally fuzzy; we print them for visual inspection.
        print("Summarization Result:", result.output)
        self.assertIsNone(result.error)

    def test_llm_based_explanation(self):
        text = "E=mc^2 is Einstein's mass-energy equivalence formula."
        result = await self.tool.execute(operation="explanation", text=text, use_llm=True)
        # LLM outputs are generally fuzzy; we print them for visual inspection.
        print("Explanation Result:", result.output)
        self.assertIsNone(result.error)

if __name__ == "__main__":
    unittest.main()
```

Now please generate the test code depend on the following tool code:
{code}

The output must be only the Python code — no explanations, comments, or markdown. Do not wrap the code block in triple backticks.

Be strict about syntax correctness. The code must be ready to run as-is via `exec()` and loadable as `TestTool` from the global namespace.
